export interface Event {
  _id: string;
  description: string;
  img: string;
  price: string;
  register: string;
  title: string;
  when: string;
  where: string;
  which: string;
  register_mail: string;
  register_phone: string;
  date: Date;
}
